int num = *it; //where it is the iterator
auto ite = upper_bound(it,S.end(),num);
int dup = distance(it,ite);
...
it = ite;
int num = *it; //where it is the iterator
auto ite = upper_bound(it,S.end(),num);
int dup = distance(it,ite);
...
it = ite;
int num = *it; //where it is the iterator
auto ite = upper_bound(it,S.end(),num);
int dup = distance(it,ite);
...
it = ite;
int num = *it; //where it is the iterator
auto ite = upper_bound(it,S.end(),num);
int dup = distance(it,ite);
...
it = ite;
int num = *it; //where it is the iterator
auto ite = upper_bound(it,S.end(),num);
int dup = distance(it,ite);
...
it = ite;
int num = *it; //where it is the iterator
auto ite = upper_bound(it,S.end(),num);
int dup = distance(it,ite);
...
it = ite;
int num = *it; //where it is the iterator
auto ite = upper_bound(it,S.end(),num);
int dup = distance(it,ite);
...
it = ite;
int num = *it; //where it is the iterator
auto ite = upper_bound(it,S.end(),num);
int dup = distance(it,ite);
...
it = ite;
